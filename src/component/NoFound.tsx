import { Grid } from '@mui/material';

const NoFound = () => {
  return (
    <Grid item xs={12}>
      <h3 style={{ textAlign: 'center' }}>No results found</h3>
    </Grid>
  );
};

export default NoFound;
